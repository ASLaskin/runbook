#these defaults match docker-compose.yml.

# Postgres
DATABASE_URL=postgres://runbook:runbook@localhost:5433/runbook

# Redis
REDIS_URL=redis://localhost:6380

# "mock" or "real" (needs ANTHROPIC_API_KEY)
LLM_MODE=mock
ANTHROPIC_API_KEY=
